ID=setup-check
TITLE=Setup Check
DESCRIPTION=Bootstrap the runtime and print setup status without opening the menu.
TIMEOUT_SECONDS=1800
CWD={{repo}}
POSIX_PROGRAM=./start.sh
POSIX_ARGS=--check-only
WINDOWS_PROGRAM=cmd
WINDOWS_ARGS=/c|start.bat|--check-only
