# smaLLMs Marathon Mode - Core Dependencies
# Optimized for local model evaluation with Ollama + LM Studio

# === CORE DEPENDENCIES ===
# HuggingFace ecosystem for benchmarks and cloud models
huggingface_hub>=0.20.0
datasets>=2.16.0
transformers>=4.36.0
evaluate>=0.4.1

# PyTorch for model operations
torch>=2.1.0

# Data processing and analysis
pandas>=2.1.0
numpy>=1.24.0

# Configuration and utilities
pyyaml>=6.0.1
requests>=2.31.0
tqdm>=4.66.0

# Async operations for Marathon Mode
aiohttp>=3.9.0
asyncio-throttle>=1.0.2

# System monitoring and resource management
psutil>=5.9.0

# === EVALUATION & METRICS ===
# Statistical analysis and metrics calculation
scikit-learn>=1.3.0
scipy>=1.11.0

# Visualization for results export
matplotlib>=3.7.0
seaborn>=0.12.0

# === OPTIONAL WEB INTERFACE ===
# Uncomment if you want the web interface
# gradio>=4.0.0
# fastapi>=0.104.0
# uvicorn>=0.24.0

# === LOCAL MODEL INTEGRATION ===
# For Ollama integration (automatically handled by requests)
# ollama  # Not needed - we use direct API calls

# For LM Studio integration (automatically handled by requests)  
# lmstudio  # Not needed - we use direct API calls

# === DEVELOPMENT DEPENDENCIES ===
# Uncomment for development work
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.6.0

# === PLATFORM NOTES ===
# Windows: All dependencies work out of the box
# macOS/Linux: All dependencies work out of the box  
# No additional system dependencies required for Marathon Mode
